<h2>商品一覧(全<%= @items.count %>件)</h2>

<% @items.each do |item| %>
  <div>
    <%= image_tag item.item_image %>
    <%= link_to item.name, item_path(item.id) %>
    ¥<%= item.price %>
  </div>
<% end %>