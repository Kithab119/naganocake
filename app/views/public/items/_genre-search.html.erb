<div class="col-3 mt-5">
  <table class="table-bordered">
    <thead>
      <tr>
        <th>ジャンル検索</th>
      </tr>
    </thead>
    <tbody>
      <% @genres.each do |genre| %>
        <tr>
          <td><%= link_to genre.name, items_path(genre_id: genre.id), class: "text-dark" %></td>
          <!--                                   任意のキー: 値-->
        </tr>
      <% end %>
    </tbody>
  </table>
</div>